{
  "name": "apexorchastrator-web",
  "version": "1.0.0",
  "private": true,
  "description": "Web dashboard + API for the ApexOrchastrator viral content pipeline, using Groq's free-tier API for AI.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
